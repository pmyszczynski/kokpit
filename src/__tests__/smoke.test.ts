// Smoke test — verifies the test runner itself is wired correctly.
// Expand this file once real unit tests exist.
describe("scaffold", () => {
  it("test runner is operational", () => {
    expect(1 + 1).toBe(2);
  });
});
